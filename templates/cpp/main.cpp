#include <bits/stdc++.h>

using namespace std;

function<void()> solve = []() {};

int main()
{
    ios::sync_with_stdio(false);
    cin.tie(nullptr);

    int TESTS = 1;
    cin >> TESTS;
    while (TESTS-- > 0)
        solve();
}
